import React from "react";

type PaginationProps = {
  name: string;
  id: number;
  bio?: string;
};

export const Pagination: React.FC<PaginationProps> = ({ name, id, bio }) => {
  return <div></div>;
};

export default Pagination;
